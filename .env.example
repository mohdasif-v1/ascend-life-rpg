# ==============================================================================
# ASCEND — Life RPG Environment Configuration Template
# Copy this file to .env.local for local development. Do NOT commit secrets.
# ==============================================================================

# MongoDB (Required for upcoming phase: Hour 0.5–1.5)
MONGODB_URI=

# NextAuth Configuration (Required for upcoming phase: Hour 0.5–1.5)
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=
